{
  "name": "check-react-env",
  "version": "1.0.0",
  "description": "Check that all used environment variables are set.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "arg": "^5.0.2",
    "fast-glob": "^3.2.12",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "prettier": "^2.8.1"
  },
  "peerDependencies": {
    "react-scripts": "5.0.0"
  },
  "bin": {
    "check-react-env": "./src/index.js"
  }
}
